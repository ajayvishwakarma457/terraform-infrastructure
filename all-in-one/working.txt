terraform destroy -target=module.vpc
terraform apply -target=module.ec2


in rds services we have so manythings to do?
but what terraform can do list me out that things






Secrets Manager
     ↓
IAM Task Role (permission)
     ↓
ECS Task Definition
     ↓
Container ENV at runtime


aws ecs execute-command \
  --cluster app-cluster \
  --task 54f6ba0ffd564c25846dbb141b2c3b1e \
  --container app-service \
  --interactive \
  --command "/bin/sh"


  echo $DB_PASSWORD
